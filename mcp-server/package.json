{
  "name": "llamadasrobin-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Servidor MCP de LlamadasRobin con lectura y escritura protegida opcional",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --import=tsx src/index.ts",
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "test": "node --test --import=tsx test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
