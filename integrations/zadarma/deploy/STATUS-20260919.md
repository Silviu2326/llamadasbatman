# Despliegue de llamadas de prueba — 19 de septiembre de 2026

Estado: DESPLEGADO y verificado el 19 de septiembre de 2026, a partir de las 12:35 UTC.

- Backend compilado con `npm run build`.
- 14 pruebas offline de llamadas de prueba, pasarela, grabaciones y worker aprobadas.
- Instalador comprobado ante incompatibilidad de Prisma y fallo del health check; la simulación de fallo restauró código y cliente Prisma completo.
- Paquete privado local: `integrations/zadarma/testcall-bundle.local.tar.gz` (336799 bytes).
- SHA256 del paquete: `9bb722fc45a1728a6d0df2ce02fe6fb300dd7701ee835343f1e71589ffc0e2cf`.
- Cliente Prisma del paquete: 5.22.0. El instalador verifica la versión del VPS y consulta `VoiceTestNumber` y `Call.isTest` antes de arrancar.
- Instalador: `integrations/zadarma/deploy/install-testcall.py`.
- SHA256 del instalador final: `be87b93ea778789e54e13ad3163933736bc6b83b01036136ebc9c8c384ef73ef`.

El usuario confirmó la nueva clave temporal. Se instaló por consola, se usó para SCP/SSH y se retiró de `authorized_keys` tras completar el despliegue. El acceso temporal ya no está disponible.

Resultado verificado:

- El instalador verificó 19 archivos del paquete, instaló 8 archivos y regeneró Prisma con la CLI del VPS.
- Copia de seguridad: `/opt/vendrava/backups/testcall-20260919T123535Z`, incluido el cliente Prisma completo anterior.
- Consultas reales de `VoiceTestNumber` y `Call.isTest` correctas, también como usuario `vendrava`. Ambas contaron cero registros.
- `vendrava-zadarma` y `vendrava-call-worker`: activos, habilitados y sin reinicios automáticos. PID 1252970 y 1252972, respectivamente.
- Heartbeat del `call-worker`: 5 segundos de antigüedad en la verificación posterior.
- Health HTTPS público autenticado: `listening`, cero llamadas activas, grabación `asterisk-mixmonitor`.
- El nuevo formato `mode: test` se probó con una organización ficticia: respondió `403 ORGANIZATION_NOT_ALLOWED`, confirmando el formato y bloqueando antes de marcar.
- Los SIP original y del agente continuaron `Registered`.
- Asterisk, nginx y Sprintmarkt conservaron sus PID y fechas de arranque: no se reiniciaron ni se modificó su configuración.
- Sin errores de los servicios Vendrava en la consulta de journal posterior.

El primer intento abortó al leer el formato binario de SHA256 de Git Bash, antes de modificar servicios. El instalador se corrigió para aceptar los dos formatos estándar y el segundo intento terminó correctamente.

No se realizaron llamadas ni se registró consentimiento de voz o números de prueba. El siguiente paso funcional es preparar esos requisitos y ejecutar una prueba autorizada; el despliegue y el cliente Prisma ya no son bloqueos.
